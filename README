AESTE FREERTOS PORT
===================

Port of FreeRTOS to AESTE platforms. The port is for both co-operative
and pre-emptive kernels. Only tested with Heap3 that uses malloc() and
needs a libc to work.
